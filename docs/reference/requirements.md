# Requirements

The functional requirements tie the corpus to machine-checkable expectations.

## Core Requirements

- Each practice must have a unique ID.
- A machine-readable practices index must exist.
- Wrap decisions must be documented and traceable.
- Top-level repo layout rules must be enforced.

## Why It Matters

These requirements make the corpus easier to validate automatically and easier to consume in agent-driven workflows.
